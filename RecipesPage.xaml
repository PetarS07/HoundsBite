<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="HoundsBite.RecipesPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Recipes">

    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="16">

            <Entry x:Name="RecipeName" Placeholder="Recipe name" />
            <Entry x:Name="RecipeType" Placeholder="Type (Breakfast/Lunch/Dinner)" />
            <Editor x:Name="RecipeDesc" Placeholder="Description" />

            <Button Text="Add Recipe" Clicked="OnAddRecipe" />

            <CollectionView x:Name="RecipeList">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <VerticalStackLayout>
                            <Label Text="{Binding Name}" FontAttributes="Bold" />
                            <Label Text="{Binding Type}" />
                            <Label Text="{Binding Description}" />
                            <Button Text="Delete" CommandParameter="{Binding .}" Clicked="OnDeleteRecipe" />
                        </VerticalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>